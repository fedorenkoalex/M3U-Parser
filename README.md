# M3U Playlist Parser Example
Example of M3U playlists parsing.

# Usage

**Copy `M3UParser.java`, `M3UPlaylist.java` and `M3UItem.java` to your project.**

**Call `parseFile(InputStream inputStream)` function of `M3UParser.java` class**


	
